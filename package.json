{
  "name": "micro-frontend-explaination",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc && node ./dist/",
    "bootstrap": "git submodule update",
    "build:vue-app": "cd vue-app && pnpm build",
    "build:react-app": "cd react-app && npm run build",
    "build:svelte-app": "cd svelte-app && npm run build",
    "build:web-component": "cd web-component && npm run build",
    "build:wc-react-app": "cd wc-react-app && npm run build",
    "build:wc-svelte-app": "cd wc-svelte-app && npm run build"
  },
  "repository": {
    "type": "git"
  },
  "author": "Son Tran",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "vite-plugin-css-injected-by-js": "^1.5.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "typescript": "^4.7.4"
  }
}
